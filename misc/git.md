# Git Notes

## Creating a Project

Create a new repository in your GitHub account. Copy the url to this repository and use within the following commands:

```
git init
git commit -m "initialized project"
git remote add origin <url>
git push -u origin main
```